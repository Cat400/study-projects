#include <stdio.h>
#include <math.h>

int main()
{
    int b;
    scanf("%d", &b);
    for(int a=2;a <= b;a++) 
    {
        int parametr=1;
        for(int c=2;c <= 3;c++)
        {
            double d=pow(c,a);
            double e=fmod(d,a);
            if((int)e == c % a) {
                 parametr++;
            }
             if(parametr==3) { 
                 
                 printf("%d\n", a);
            }
        }
    }
}
