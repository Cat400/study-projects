#include <stdio.h>

#include <math.h>

 

 int main()
{
    
    int b;
    
     scanf("%d", &b);
    
    
    for(int a=2;a<=b;a++) {
        
        int p=1;
        
        for(int c=2;c<=3;c++){
            
            double d=pow(c,a);
            
            double e=fmod(d,a);
            
            int f=e;
             
             if(f == c%a) {
                 
                 p++;
                 
             }
             
             if(p==3) { 
                 
                 
                 printf("%d\n", a);
             }
            
    
        }
        }
        
}
